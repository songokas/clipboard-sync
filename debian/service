[Unit]
Description=Secure clipboard sync across devices

[Service]
ExecStart=/usr/bin/clipboard-sync --config %h/.config/clipboard-sync/config.yml

NoNewPrivileges=true

[Install]
WantedBy=multi-user.target